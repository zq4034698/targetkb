import type { Metadata } from 'next';
import ExactSizeLanding from '../components/ExactSizeLanding';

export const metadata: Metadata = { title: 'Compress Image to 200KB — Free Online | TargetKB', description: 'Compress images to 200KB online. Keep photos clear while meeting image upload limits.', alternates: { canonical: '/compress-image-to-200kb' } };

export default function Page() { return <ExactSizeLanding size="200KB" query="200" title="Compress image to 200KB" description="Make photos fit a 200KB limit with automatic compression that keeps detail where it matters." uses={[{ heading: 'Profile photos', text: 'Prepare a clear, compact image for registrations and member portals.' }, { heading: 'School applications', text: 'Keep application images within a typical attachment limit.' }, { heading: 'Email attachments', text: 'Share several clear photos while keeping the total message size manageable.' }]} guide="TargetKB uses the 200KB ceiling as the goal, then finds the best combination of image dimensions and quality. You get a compatible JPG without manually testing different settings." relatedGuide={{ href: '/guides/how-to-compress-image-to-200kb', label: 'Read the full guide: how to compress an image to 200KB' }} faqs={[{ question: 'Can I compress multiple images to 200KB?', answer: 'Yes. Upload up to 10 images and TargetKB will process each one with the same 200KB limit.' }, { question: 'Does compressing to 200KB crop my image?', answer: 'No. The compressor preserves your image aspect ratio. It may reduce resolution when needed to meet the file limit.' }, { question: 'Which formats can I upload?', answer: 'TargetKB accepts JPG, PNG, WebP and HEIC images when your browser supports the format.' }]} />; }
